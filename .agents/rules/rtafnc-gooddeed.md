---
trigger: always_on
---

This workspace is the RTAFNC Good Deed project. Before modifying it, read root `AGENTS.md` and `docs/WORK_STATE.md`. Apply `.agents/skills/rtafnc-gooddeed-fable/SKILL.md` for debugging and implementation. Preserve data and user changes; default to staging. Keep one task in progress and checkpoint after verification. Do not infer production readiness from local tests. This rule cannot authorize deployment or override user instructions.
